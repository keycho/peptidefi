# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.next/
out/
.turbo/

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# testing
coverage/
.nyc_output/

# supabase local link state (keeps project ref / access token out of git)
.supabase/
**/.supabase/

# typescript build info
*.tsbuildinfo

# playwright
test-results/
playwright-report/
playwright/.cache/

# next.js
.next/
next-env.d.ts

# rust / cargo build artifacts (sandbox runs `cargo check` against the
# peg program if it's checked out on this branch; never commit target/).
target/
**/*.rs.bk

# anchor: local IDL extract dir + the disposable local-validator ledger
# the anchor test harness spins up. Both are workspace-build artifacts;
# the vendored production IDL lives at apps/oracle/src/index/idl.json.
.anchor/
test-ledger/

# scripts/initialize-peg-mainnet.ts outputs (operator-machine-only;
# contain the $bBPC157 mint keypair + signed-transaction journal).
scripts/peg-mainnet-init-output.json
scripts/bbpc157-mint-keypair.json

# foundry build artifacts + installed deps (programs/base)
programs/base/out/
programs/base/cache/
programs/base/lib/
programs/base/broadcast/

# biohash_index_lz_emitter deploy script output (deployment journal,
# operator-machine-only)
programs/biohash_index_lz_emitter/scripts/init-output.json
